<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="PopupMenuSmaller" parent="@style/Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@android:color/white</item>
        <item name="android:textSize">10sp</item> <!-- Adjust the text size as desired -->
        <item name="android:itemTextAppearance">@style/PopupMenuTextAppearance</item>
    </style>

    <style name="PopupMenuTextAppearance" parent="@android:style/TextAppearance.Widget.PopupMenu.Large">
        <item name="android:textColor">#000000</item> <!-- Adjust the text color as desired -->
    </style>

</resources>